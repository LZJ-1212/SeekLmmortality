---
description: 改代码或文档必须追加署名与北京时间；多人协作底线。
alwaysApply: true
---

修订：2026-09-02 21:37 +08 lzj — 新建署名规则

# 修订署名（底层行动）

改 **代码、文档、新建文件** 时，必须在该文件里留下署名和时间。规格全文：[docs/attribution.md](../../docs/attribution.md)。Git 提交不能代替文件内修订行。

## 署名

| 署名 | 谁 |
|------|-----|
| `lzj` | 主人；当前分支 `lzj` |
| `Loading_line` | 朋友；当前分支 `Loading_line` |

其它人用 Git `user.name` 短名。时间用 **北京时间** `YYYY-MM-DD HH:mm +08`（以会话时间戳为准）。

## 必做

- **新文件**：写「创建」一行。
- **改已有文件**：在修订区 **追加** 一行，不删旧行；超过 8 行只留最近 8 行。
- 说明用一句中文（做了什么），不要写「微调」。
- 同一回合改同一文件只追加 **一行**。

## 格式

文档（标题下）：

```
修订：2026-09-02 21:37 +08 lzj — 新建署名规范
```

多行时用表：`时间 | 作者 | 说明`。

`.ts` / `.tsx` 文件顶部注释：

```
/** 修订：2026-09-02 21:37 +08 lzj — 自定义物按名消耗 */
```

已有修订块则在块内追加一行，不要每函数再签一次。

## 不要签

`node_modules/`、`frontend/dist/`、`backend/generated/`、`.env`、锁文件、Prisma 生成客户端。
