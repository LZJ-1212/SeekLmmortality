# 修订：2026-09-05 01:31 +08 lzj — 更新服叠在 .env 上的端口与库名
# 仅 `npm run dev:update` 读取。复制为 `.env.update`（已 gitignore）。
# 不要把 DeepSeek Key、口令写进本示例。

PORT=3001
DATABASE_URL="mysql://root:@localhost:3306/wendaocs_dev"
