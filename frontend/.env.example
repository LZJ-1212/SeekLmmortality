# 前端环境变量示例（复制为 .env.local 后按需填写；.env.local 已被 gitignore）

# 本机开发：留空或不设（等价于 http://localhost:3000）
# 拓扑 B 双隧道：填 API 公网 Origin（无尾斜杠），例如 https://api.example.tld
# 拓扑 A 同源反代：显式设为空字符串，走相对路径 /api/...
# VITE_API_BASE=
